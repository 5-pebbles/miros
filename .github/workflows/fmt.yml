name: Format

on:
  push:
    branches: [main]
  pull_request:

jobs:
  fmt:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # rustup installs the pinned toolchain from rust-toolchain.toml (rustfmt included) on first use.
      - run: cargo fmt -- --check
